# Phi

An open-source AI coding agent that connects to your claude code subscription.

## Authenticate

Run `claude setup-token` to get the token, and set it as the `ANTHROPIC_AUTH_TOKEN` environment variable.
